# Local game data

Put your extracted `game-data.json` here as:

```text
data/game-data.json
```

The real file is intentionally ignored by Git because it comes from the game
assets and should not be published accidentally. You can also pass the path
manually with:

```powershell
python app.py --game-data "C:\path-to-game-data.json"
```
